#include<iostream>
#include<queue>
using namespace std;

int main(){
    deque<int> q;

    q.push_front(1);
    cout<<"top back "<<q.back()<<endl;
   cout<<"top  front "<<q.front()<<endl;

    q.push_front(2);
   cout<<"top back "<<q.back()<<endl;
   cout<<"top front "<<q.front()<<endl;

   q.pop_back();
   cout<<"top back "<<q.back()<<endl;
   cout<<"top front "<<q.front()<<endl;


if(q.empty())
{
    cout<<"empty"<<endl;
}
else{
    cout<<"not empty"<<endl;
}
   
   
   
   
    /*
    queue<int> q;
    q.push(1); 
    cout<<"top"<<q.front()<<endl;
    cout<<"size"<<q.size()<<endl;

    q.push(3);
    cout<<"top"<<q.front()<<endl;  
    cout<<"size"<<q.size()<<endl;

    q.pop();
    cout<<"top"<<q.front()<<endl;

    cout<<"size"<<q.size()<<endl;



    if(q.empty()){
        cout<<"empty";
    }
    else{
        cout<<" not empty";
    }
 */
    return 0;

}
